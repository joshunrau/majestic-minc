PROJECT(MINC_GCO CXX C)


CMAKE_MINIMUM_REQUIRED(VERSION 3.1)

IF(NOT MINC_TOOLKIT_BUILD)
  FIND_PACKAGE(LIBMINC REQUIRED)
  
  INCLUDE( ${LIBMINC_USE_FILE} )
ENDIF(NOT MINC_TOOLKIT_BUILD)


ADD_SUBDIRECTORY(src)

